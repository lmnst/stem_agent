# Changelog

## v1.0 - Standalone release

- Renamed package `stem_agent` -> `blueprint_repair`; CLI is now
  `python -m blueprint_repair.cli`.
- Rewrote README top section with headline results table, Wilson 95% CIs,
  budget-controlled attempts, and challenge split boundary analysis.
- Added repo metadata file (`.github/REPO_METADATA.md`) with description and
  topics to be set on github.com.
- No changes to pipeline logic, benchmark, tests, or the committed
  perturbation report.
